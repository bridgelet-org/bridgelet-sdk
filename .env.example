# Application
NODE_ENV=development
PORT=3000
APP_NAME=Bridgelet SDK

# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=bridgelet
DATABASE_USER=bridgelet_user
DATABASE_PASSWORD=bridgelet_pass
DATABASE_SYNC=false
DATABASE_LOGGING=true

# Stellar
STELLAR_NETWORK=testnet
STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
STELLAR_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Stellar Accounts
FUNDING_ACCOUNT_SECRET=SXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
RECOVERY_ACCOUNT_PUBLIC=GXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Smart Contracts
EPHEMERAL_ACCOUNT_CONTRACT_ID=CXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
SWEEP_CONTROLLER_CONTRACT_ID=CXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# Version of the deployed ephemeral-account contract, reported by
# ContractProvider.getContractInfo(). Bump this whenever the contract is
# redeployed; if unset, getContractInfo() reports 'unknown'.
EPHEMERAL_ACCOUNT_CONTRACT_VERSION=0.1.0

# 32-byte Ed25519 seed for sweep authorization signing (hex-encoded)
# The corresponding public key must be registered in SweepController as authorized_signer
# It goes nowhere near the chain and nowhere near AUTHORIZED_SIGNER_PUBLIC_KEY
# Generated with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# .........................................................................................................
# The public key it derives → goes to AUTHORIZED_SIGNER_PUBLIC_KEY
# Run this now to get it:
# ./target/release/sweep-signer pubkey --signer-seed-hex d06108fa393494747d37380f0b8a044d641c0e8c325f622fd3dacaff7376febd
SWEEP_SIGNING_KEY_SEED=

# Security
JWT_SECRET=your-super-secret-jwt-key-change-in-production
CLAIM_TOKEN_EXPIRY=2592000

# Previous JWT secret, accepted only while rotating JWT_SECRET. Claim tokens
# are valid for up to CLAIM_TOKEN_EXPIRY and cannot be re-issued (only a hash
# is stored), so keep the previous secret accepted for at least that long or
# every unclaimed token in flight is stranded. Remove once the window closes.
# See docs/jwt-secret-rotation-runbook.md.
JWT_SECRET_PREVIOUS=

# Secret encryption — fallback AES-256-GCM key used only when KMS is disabled
# (KMS_ENABLED=false) or unavailable. Not used in production; see SECURITY.md.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-64-character-hex-string-here

# Previous AES-256 key, kept readable while rotating ENCRYPTION_KEY so rows
# written under the old key still decrypt. Leave a rotation window open (at
# least as long as the longest-lived account), re-encrypt with
# `npm run migrate:secrets`, then clear it. See SECURITY.md.
ENCRYPTION_KEY_PREVIOUS=

# Key id written into `aes256gcm:v2:<keyId>:...` ciphertext. Set this to opt
# into key-id tagged writes during a rotation; leave empty to keep writing the
# v1 format.
ENCRYPTION_KEY_ID=

# AWS KMS — envelope encryption for secret keys (recommended for production)
KMS_ENABLED=true
KMS_KEY_ID=
AWS_REGION=us-east-1

# Persistence for the KMS-wrapped data key. Without one of these the data key
# is regenerated on every restart, which makes existing secrets
# undecryptable. Set KMS_DATA_KEY_PATH to a writable path, or supply
# KMS_ENCRYPTED_DATA_KEY (base64 blob) from a durable store such as AWS
# Systems Manager Parameter Store or Secrets Manager.
KMS_DATA_KEY_PATH=.kms-data-key
# KMS_ENCRYPTED_DATA_KEY=

# API
API_RATE_LIMIT=100

# Payment Monitor — how often to poll Horizon for inbound payments (ms)
PAYMENT_POLL_INTERVAL_MS=30000

# Webhooks
WEBHOOK_RETRY_ATTEMPTS=3
WEBHOOK_TIMEOUT=5000

# Scheduler
EXPIRY_CHECK_INTERVAL_MS=300000        # 5 minutes
INITIALIZING_TIMEOUT_MS=600000         # 10 minutes — stale INITIALIZING accounts marked FAILED after this
INITIALIZING_CLEANUP_INTERVAL_MS=900000 # 15 minutes

# Logging
LOG_LEVEL=debug
